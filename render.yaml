services:
  - type: web
    name: lords-mobile-translator-api
    runtime: docker
    repo: https://github.com
    plan: free
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
